document.addEventListener("DOMContentLoaded", function(e) {
    /**Es para ocultar el boton de agregar bonos - se activa cuando se hayan agregado los archivos */

    /*
    var mostrarArchivos = document.getElementById('archivos')

    if ( mostrarArchivos !== null) {
        document.getElementById('btnAgregar').setAttribute("enable", "true");
        console.log("exiten archivos")
    }else{
        document.getElementById('btnAgregar').setAttribute("disabled", "true");
        console.log("no existen archivos")
    }
    */
   
});